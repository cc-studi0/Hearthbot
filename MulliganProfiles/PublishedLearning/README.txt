Published mulligan memory snapshots live here.
They are generated by tools/bootstrap_local_strategy_memory.py from the repo's existing strategy assets.
Entries: 30708
Source rows: 60635

Source files:
- MulliganProfiles/Standard/mulligan.json
- MulliganProfiles/Wild/mulligan.json
- MulliganProfiles/Arena/mulligan.json
- MulliganProfiles/Standard/MulliganDecks.json
- MulliganProfiles/Wild/MulliganDecks.json
- MulliganProfiles/Arena/MulliganDecks.json

Legacy dependency:
Primary box-learning mulligan path already consumes memory first. UniversalMulligan can now read PublishedLearning first when pure_learning_mode is enabled, but will still fall back to mulligan.json/MulliganDecks.json when legacy mulligan fallback stays enabled. Keep source files until live logs confirm the old JSON path is no longer used.

Cutover switch:
runtime/learning/decision_runtime_mode.json -> pure_learning_mode=true, allow_legacy_mulligan_fallback=false

Retirement gate:
Delete legacy strategy files only after live logs confirm memory decisions are stable and no runtime path still depends on the original JSON assets.
